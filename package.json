{
  "name": "sse-reader",
  "version": "1.0.0",
  "description": "Simple SSE reader",
  "scripts": {
    "start:node": "ts-node src/index.ts",
    "start:browser": "npx http-server ./src -p 8080 -c-1 --cors"
  },
  "dependencies": {
    "eventsource": "^2.0.2"
  },
  "devDependencies": {
    "@types/eventsource": "^1.1.15",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}